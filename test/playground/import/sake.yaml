disable_verify_host: true

import:
  - ./imports/sake-1.yaml

servers:
  localhost:
    host: localhost
    local: true

themes:
  default:
    text:
      prefix: true

tasks:
  ping:
    desc: kaka
    spec: table
    target: all
    cmd: echo $ping
